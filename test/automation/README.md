# Comet Studio Automation Package

This package contains the reusable Playwright driver and Comet Studio page objects used by `test/smoke`.

The workbench-side driver contract lives in `src/cs/workbench/services/driver/common/driver.ts` and is copied into this package during compilation.
