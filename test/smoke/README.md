# Comet Studio Smoke Tests

These tests launch the built Electron application through `test/automation` and exercise user-visible workbench flows.

Run all smoke tests from the repository root with `npm run test:smoke`.
